# 老鄰居 LINE Bot（Python 版）交班日志

用途：每次工作結束前追加一筆，讓下一個 Claude 快速接手。

---

## 專案基本資料

主程式：D:\Desktop\line-bot\app.py
GitHub：bjniketown/line-bot
線上服務：https://line-bot-endb.onrender.com
後台入口：https://line-bot-endb.onrender.com/store?token=Xl3xup6rm@
訂單後台：https://line-bot-endb.onrender.com/orders?token=Xl3xup6rm@
最近對話：https://line-bot-endb.onrender.com/recent?token=Xl3xup6rm@

---

## 工作規則

- 只編輯 D:\Desktop\line-bot\app.py
- 只有使用者說「上傳」才 commit + push
- Python 指令一律用 `py`，不用 `python` 或 `python3`
- 修改前必須先 Read/Grep 確認程式碼，不可猜測

---

## 日志紀錄

### 2026-05-31 A — 修正 confirm_customer_data 流程兩個 bug

**commit：** bc06101

**做了什麼：**
在 SYSTEM_TEXT 的 `confirm_customer_data` 工具說明新增兩條規則：
1. 補問資料後必須重呼叫：客人補充新資料後，必須再次呼叫 `confirm_customer_data`，不得直接呼叫 `create_pickup`（防止遮罩電話流入訂單）
2. 門市自取不需全名：自取訂單單字姓氏可接受，不得要求完整全名

**觸發原因：**
真實案例——楊欣儒（U4a7ebb8dacf5f75756b95bcc2d3ee251）訂門市自取，bot 補問姓名後直接呼叫 `create_pickup` 傳入遮罩電話 `0980****98`，訂單失敗。

**未完全解決的問題：**
內測時發現另一個更根本的 bug：對話歷史有失敗紀錄時，bot 在客人說「相同」後不呼叫 `confirm_customer_data`，而是自己說「系統無法直接讀取電話」並直接問電話。這個問題尚未修正。

---

### 2026-05-31 B — 後台搜尋對話記錄移除 120 字截斷

**commit：** 9c56ebc

**做了什麼：**
`/recent` 後台搜尋結果的訊息顯示從 `[:120]` 改為完整顯示，並加上 `white-space:pre-wrap` 讓換行正常顯示。

**觸發原因：**
訂單確認訊息 173 字，後台注入記憶時只看到截斷版本，無法確認內容是否正確。

---

---

### 2026-06-01 A — 修正「去拿」未識別為自取 + 回訪客無地址預設自取

**commit：** 2aec2cc

**做了什麼：**
1. SYSTEM_TEXT 新增「去拿」「去取」「親取」等詞視為門市自取，不可詢問地址
2. 回訪客 `has_address=false` 時，預設門市自取，不詢問宅配或自取、不問地址，直接問品項與取貨時間。若客人主動說「要宅配」才切換宅配流程。

**觸發原因：**
真實案例——客人說「今天5:20去拿」，bot 誤判為宅配並追問地址，訂單卡住。

**內測結果：**
用業主 UID 模擬，客人說「對（資料相同）」後，bot 直接問品項與取貨時間，沒有問宅配/自取或地址。PASS。
真實訂單（何安馨）同日成功完成，confirm_customer_data → validate_pickup_time + calc_pickup → create_pickup 一氣呵成。

---

### 2026-06-01 B — 客戶資料重複問題（暫觀察）

**狀態：** 已知問題，暫不修，手動刪除頂著

**問題描述：**
客人之前建檔時沒有 `line_uid`，今天傳訊息後 `_fetch_and_save_line_profile` 用 `line_uid` 查不到，建了一筆假電話（`line_{uid[:12]}`）記錄，與原有真實電話記錄並存，變成兩筆。

**暫時處理：** 業主手動在 Supabase 刪除假電話那筆。

**根治方向（待實作）：** `_fetch_and_save_line_profile` 查不到 `line_uid` 時，不直接新建，改為補綁 `line_uid` 到現有記錄。

---

---

### 2026-06-01 C — 純詢問商品不追問取貨方式

**commit：** fa250fc

**做了什麼：**
修改 SYSTEM_TEXT 購買意願判斷規則：只有客人明確說「我要訂」「我要買」「下單」等才進入訂單流程。純詢問商品資訊（問價格、規格、保存方式）回答後自然帶一句「有興趣隨時告訴我要訂購喔！」，不主動追問取貨方式。

**觸發原因：**
真實案例——張玉珊詢問招牌豆干絲、天然昆布、水餃價格，bot 每則回覆後都追問「請問是門市自取還是宅配到府？」，體驗很差。

---

---

### 2026-06-02 A — 日期對照表延長 + 禁止自行推算星期

**commit：** 0a9362d（規則）、822cce0（對照表）

**做了什麼：**
1. 日期星期對照表從 30 天延長至 60 天
2. SYSTEM_TEXT 明確禁止：客人說日期但未給時間，不可自行推算星期幾，直接問時間，由 `validate_pickup_time` 工具驗證

**觸發原因：**
真實案例——客人說「7/11」，bot 自行算出「7/11 是週五」（實際是週六），造成客人困惑。

---

### 2026-06-02 B — CRM 假電話重複問題根治

**commit：** 0da86a5

**做了什麼：**
`save_customer_profile` 存入真實電話後，直接用 `line_uid` 刪除同一客人的假電話記錄（`phone LIKE line_%`）。不再依賴舊的 `fake_record` 判斷邏輯。

批次清除現有 41 筆假電話中 4 筆有真實記錄的重複（廣德崇浦/洪崇浦、郭宜佳、李秀鈴、王小姐），保留 35 筆純訪客。

---

### 2026-06-02 C — chat_logs 全量儲存 + 人格分析調整

**commit：** b4bd206

**做了什麼：**
1. 所有 user 訊息（包含短訊息如「好」「對」）全部存入 chat_logs，供爭議查閱與未來訓練使用
2. 人格分析觸發條件：30 → 50 則
3. 人格分析取樣：60 → 100 則

---

### 2026-06-02 D — CRM 月報新增好友統計拆分

**commit：** c151e23

**做了什麼：**
/report 好友統計改為新增/封鎖/淨成長三欄，自動計算淨成長。

---

### 2026-06-02 E — 手動補建訂單

**鳳梨客人（U019393326700c0e6518d91a3256d099e）：**
- 對話混亂導致訂單從未成功建立，客人先付款後才發現沒有訂單記錄
- 手動補建：陳宜君 / 0918873219 / 豆干絲50包 3,500元 / 出貨 2026-06-03
- 清除 Redis 對話歷史，注入訂單記錄與匯款確認兩則記憶

---

---

### 2026-06-07 A — 手動補建訂單（讚讚讚阿姨 480元）

**做了什麼：**
讚讚讚阿姨（U73a627f51fb3c5ca1dc9095b4672d1ac，0953936282）今天 09:07 下了一張自取單（豆干絲8份×60元=480元，取貨 10:00），但機器人呼叫 `calc_pickup` 後直接輸出確認訊息，**跳過了 `create_pickup`**，導致 Redis 和 Supabase 都沒有記錄。

手動補建 Supabase 訂單 #86，Redis 不補（取貨時間已過）。

**根本原因：** 機器人未遵守「calc_pickup 後必須呼叫 create_pickup」規則，無程式 bug。

---

### 2026-06-07 B — 新客戶流程不重複詢問已知資訊

**commit：** 921d1b0

**做了什麼：**
1. SYSTEM_TEXT 流程說明：`found=false` 後先回顧對話已知資訊（品項、取貨時間、姓名、電話），已知者不重問，只補問缺少的
2. `get_customer_profile` 工具說明：同步補上「已知資訊不重問」規則

**觸發原因：**
余小姐（Ue60249db6df5628725e6421120de08d3）對話中，客人第一則就說「3包今天1:00左右取貨」，機器人查無資料後仍重新問品項與數量，完全無視對話歷史。

---

### 2026-06-07 C — 修正簡訊導入舊客戶被誤判為新客戶

**commit：** d04d520

**做了什麼：**
1. `get_customer_profile` 工具：第一次用 UID 查無資料時，回傳訊息改為「請勿宣告新客戶，先詢問電話再查一次」；只有帶電話第二次查仍查無，才回傳「確認為新客戶」
2. SYSTEM_TEXT：`found=false` 的兩種情境（phone 為空 / phone 已提供）明確拆分說明

**觸發原因：**
業主正在透過簡訊廣發平台將舊客戶導入 LINE，CRM 有舊資料但尚未綁定 UID。機器人第一次查無 UID 就說「新朋友」，造成舊客戶體驗差。

---

### 2026-06-07 D — 快取回覆補 [CACHE] log

**commit：** 434589f

**做了什麼：**
快取命中的回覆原本不產生任何 log，Render 看不到機器人回了什麼。在 `ask_with_cache` 快取命中的 `return` 前加入 `log.info(f"[CACHE] uid={uid} ...")`，格式與 `[RAW]` 一致。

**觸發原因：**
鍾純貞對話分析時發現「多少錢？可以免運費？」有回覆但 log 沒有記錄，確認是快取攔截造成，補上後所有對話回覆都有完整 log。

---

### 2026-06-07 E — SYSTEM_TEXT 精簡整理

**commit：** c1a361c

**做了什麼：**
1. 修正重複規則編號：原本有兩個 17、兩個 19，重新排序為 17–23
2. 刪除 FAQ 區塊 19 條：全部已被上方結構化規則覆蓋，屬重複內容（運費計算、混搭、FAQ 等）
3. 合併 get_customer_profile 說明：刪除 rule 8 區域的重複章節（呼叫時機 ①②③），保留並更完整的【客人資料比對】段落

**驗證方式：**
逐條比對刪除內容與保留內容，確認每一條規則都有對應覆蓋，機器人判斷邏輯不受影響。

---

### 2026-06-08 A — 新客戶確認後不重複詢問已知電話

**commit：** 3607178

**做了什麼：**
SYSTEM_TEXT line 1780，found=false 確認新客後的「已知資訊不重問」列舉，從「品項、取貨時間、姓名」補上「電話」。

**觸發原因：**
王利丹（U4520cf6c3a96ebf7954d39c864182aba）已提供電話 0921933251，機器人確認新客後仍再問一次電話。

---

### 2026-06-08 B — 移除運費/免運關鍵字靜態回覆

**commit：** 75d853a

**做了什麼：**
1. 從 KEYWORD_RULES 移除「🚚 運費免運」條目（運費、免運兩個關鍵字）
2. 同步清除針對運費的排除條件（含數字跳過、修改+運費跳過）

**觸發原因：**
黃明俊（U893a683eec667bf9dc1c16b010478e95）說「先幫我算運費」，因無數字沒被排除，觸發靜態規則說明，沒有實際計算。移除後由 Claude 依對話脈絡處理，訂購中問運費可直接呼叫 calc_delivery 計算。

---

### 2026-06-08 C — 地址不完整追問邏輯修正

**commit：** b28922b

**做了什麼：**
SYSTEM_TEXT 8d 補全地址各層級對應的追問方式：缺縣市區→問縣市區、缺門牌→問門牌、缺路街+門牌→一起問。

**觸發原因：**
林靜婉（Uaa3d1857bdac0aeddb6b9218e74e1395）給「五權西路二段836號7樓之一」，機器人判斷缺縣市區卻問「請問門牌號碼是幾號呢？」，問錯方向。

---

### 2026-06-08 D — 第一次回覆訂購意願時加入分段訊息提示

**commit：** 7c5b2dc

**做了什麼：**
SYSTEM_TEXT rule 8，第一次回覆訂購意願時，自然帶上：「小提醒：一次告訴我品項、數量和取貨方式，我可以更快幫您處理喔 😊」

**觸發原因：**
客人習慣分段傳訊，debounce 2 秒已是最佳技術對策，加提示引導客人行為作為輔助。

---

### 2026-06-08 E — Debounce 精簡 + 分段傳送即時提示

**commit：** 875c32d

**做了什麼：**
1. Rich menu 9 個關鍵字（我要訂購/豆干絲/油潑辣子/香滷花生/產品消息/天然昆布/招牌豆干絲/門市地址/推薦好友）維持 debounce 2 秒合併
2. 一般訊息（打字、電話、地址等）跳過 debounce，立即處理
3. 一般訊息 5 秒內連續傳送，自動 push「煩請您將訊息整合，不要分段式傳送，我理解的較慢 🙏」

**觸發原因：**
Rich menu 按鈕才是分段傳送主要來源；一般打字訊息不應因 debounce 延遲回覆。

---

### 2026-06-09 A — 新增「宅配問題」關鍵字靜態回覆

**commit：** 322df8a

**做了什麼：**
在 KEYWORD_RULES 新增「宅配問題」精確比對，按 rich menu 後直接回覆宅配懶人包：
- 物流：黑貓冷凍宅配
- 運費：未滿38包$225 / 39-49包$290 / 50倍數免運
- 到貨：約定出貨日隔天（非即時出貨）
- 出貨日：可告知希望日期，以店家確認為主
- 宅配單號說明

**觸發原因：**
分析真實對話發現多位客人按「宅配問題」按鈕後，機器人誤判為有問題要投訴，雙方陷入問答迴圈，改為靜態懶人包最有效率。

---

### 2026-06-09 B — 新增機器人回覆總開關（bot_paused）

**commit：** 648dd32

**做了什麼：**
1. webhook 入口新增全域攔截：`if _redis(["GET", "bot_paused"]): continue`，所有人傳訊息一律不回覆
2. `/store` 後台新增「機器人回覆」區塊，顯示目前狀態，一鍵「暫停回覆」/「恢復回覆」
3. 暫停期間 UID 仍會記錄到 Redis `customers` SET，方便之後注入記憶

**觸發原因：**
業主店休時使用 LINE OA 自動回覆，希望機器人靜默但持續記錄 UID，之後開啟回覆可補注入對話記憶。

---

### 2026-06-09 D — 日期對照表加入節日標注

**commit：** 1cbcefe

**做了什麼：**
`current_date_text()` 的 60 天日期對照表新增節日標注，格式如 `06/19(星期五/端午節)`。
加入 2026 年全年節日：元旦、春節、和平紀念日、兒童節、清明節、勞動節、端午節、中元節、中秋節、國慶日、冬至、行憲紀念日。

**觸發原因：**
客人說「端午前到得了嗎？」機器人需要知道端午是哪天才能正確回答。

**注意：**
節日清單（`_HOLIDAYS_2026`）需每年 12 月底手動更新一次。

---

### 2026-06-09 C — 記憶化 Supabase 寫入與 Redis 注入標準流程

**commit：** 無（記憶更新）

**做了什麼：**
將手動操作 Supabase 與 Redis 的正確流程存入 Claude 記憶：
1. Supabase 寫入：先建 customers 再建 orders（外鍵限制）、欄位說明、urllib 標準腳本
2. Redis 注入：GET→append→SET 流程、hist:{uid} 格式、TTL 604800、輸出寫檔避免亂碼

**觸發原因：**
每次手動操作都重複試錯，浪費時間與 token，記憶化後直接套用。

---

### 2026-06-10 B — API 成本分析

**commit：** 無（分析工作）

**做了什麼：**
1. 確認 Anthropic API 每月約 600 TWD 的成本來源
2. 查看 Console 用量：cache read ratio 95.5%（caching 正常）、7天 uncached 783K tokens、cache read 16.7M tokens
3. 分析 max_tokens 設定（150~600，合理）
4. 結論：成本主要來自 output tokens（約 3.1M/週），屬正常範圍，無異常浪費

**觸發原因：**
業主詢問每月 600 TWD API 費用是否可降低，分析後確認系統效率正常，決定繼續使用。

---

### 2026-06-10 A — 月報沉睡客喚醒統計條件修正

**做了什麼：**
移除沉睡客喚醒的「本月需有訂單」條件，改為「created_at 在本月前（舊客戶）且本月 updated_at 有綁定 UID」即算喚醒。同時刪除多餘的 `older_phones2` Supabase 查詢（省一次 API 呼叫）。

**觸發原因：**
原本邏輯要求 `cur_phones_set`（本月有訂單）才算喚醒，但 UID 綁定本身就是下訂單的結果，不需要額外再驗證本月訂單，造成重複計算條件、實際數字偏低。

---

### 2026-06-10 C — 禁止用當下時刻判斷今天自取時間

**commit：** 1e383d8

**做了什麼：**
SYSTEM_TEXT rule 12，將原本只禁止「今天以外」用當下時刻判斷的規則，擴大為「所有日期」，並新增今天的反例：
「早上 07:13，客人說『今天早上 11 點』→ 11:00 在 08:00–13:30 內 → 直接呼叫 validate_pickup_time，絕對不可說『目前門市還在準備中』或『08:00 才開始營業』。」

**觸發原因：**
楊欣儒（U4a7ebb8dacf5f75756b95bcc2d3ee251）07:13 說「今天早上11點」，機器人見當下未到 08:00，自行回覆「目前門市還在準備中，今天早上 08:00 才開始營業」，完全未呼叫 validate_pickup_time。業主手動注入訂單處理。

### 2026-06-10 D — 建立本機自動內測流程

**commit：** 無（工具建立）

**做了什麼：**
1. 將 ANTHROPIC_KEY 存入 D:\Desktop\line-bot\.env，本機可直接呼叫 Claude API
2. 建立自動測試腳本範本：設定假環境變數 → import app → 注入對話歷史 → 呼叫 `_call_claude` → 比對禁止詞
3. 用此方式驗證 2026-06-10 C 的修正有效（PASS：機器人呼叫 get_customer_profile，未出現「準備中」等禁止詞）

**測試腳本重點：**
- 假環境變數：LINE_TOKEN/LINE_SECRET/UPSTASH_URL/SUPABASE_URL 全設空或 dummy，Redis/Supabase 自動靜默跳過
- 輸出用 `Path('result.txt').write_text(..., encoding='utf-8')` 寫檔，避免終端亂碼
- 測試完畢後刪除腳本，不留在 repo

---

### 2026-06-10 E — 手動補建訂單（陳宜君 #110）

**做了什麼：**
陳宜君（U019393326700c0e6518d91a3256d099e，0918873219）今天下了宅配單，機器人對話中說「訅單已成立」但未寫入 Supabase。
手動建立訂單 #110：豆干絲 50包 × 70元 = 3,500元，出貨日 2026-06-24，收件日 2026-06-25，地址：台中市西屯區台灣大道三段99號文心樓B1外送區台中市政府。

備註：詹益全（Ua797ab8c1f737df51437926ea59b56ed）的 Redis 記錄是業主本人協助陳宜君輸入，並非詹益全自己的訂單，不需補建。

---

### 2026-06-11 A — 修正沉睡客喚醒卡片說明文字

**commit：** 6a35861

**做了什麼：**
月報後台卡片說明文字從「本月綁定UID且有訂單的舊客」改為「本月綁定UID的舊客戶」。

**觸發原因：**
2026-06-10 A 修正喚醒判斷邏輯（移除「本月需有訂單」條件）後，卡片說明文字未同步更新，與實際邏輯不符。

---

### 2026-06-11 B — /recent 頁面載入速度優化

**commit：** 0237a55

**做了什麼：**
將 `/recent` 查詢 LINE 顯示名稱的方式從「每個 UID 各呼叫一次 Supabase」改為「一次批次查全部 10 個 UID」，只有 Supabase 查不到的才打 LINE API。

**觸發原因：**
緊急單點暫停機器人時需要開 `/recent` 找客人，但頁面載入慢（3–4 秒），緊急情況來不及操作。

**效果：**
實測飛速，因為有對話的客人幾乎都有 display_name 存在 Supabase，LINE API 幾乎不需要打。

---

### 2026-06-11 C — 手動補建訂單（林淑莉 #111）

**做了什麼：**
林淑莉（U7343c395a34af794fb82008252c3cee6，0926193831）2026-06-10 10:40 下自取單，機器人回覆「訂單已成立」但未寫入 Supabase。
手動補建訂單 #111：豆干絲（真空）7包＋香滷花生2份＋天然昆布1份＋水餃2包，總金額 1,300元，取貨 2026-06-10 12:00，status=completed。

---

### 2026-06-11 D — 手動補建訂單（楊欣儒 #112）

**做了什麼：**
楊欣儒（U4a7ebb8dacf5f75756b95bcc2d3ee251，0980585498）2026-06-10 07:18 業主手動注入自取單（豆干絲一般70包，4,200元，取貨 11:00），Supabase 無記錄，手動補建訂單 #112，status=completed。

**根本原因：**
機器人 07:13 誤說「門市還在準備中」（即 2026-06-10 C 修正的 bug），業主改以人工留言方式注入訂單確認，但未寫入 Supabase。

---

### 2026-06-11 E — 查核 6/10 全日訂單完整性

**做了什麼：**
掃描 Redis 所有對話記錄，找出 6/10 有訂單確認的對話共 5 筆，逐一比對 Supabase 是否有記錄：
- 楊欣儒 #112：補建（見 2026-06-11 D）
- 林淑莉 #111：補建（見 2026-06-11 C）
- 陳宜君 #110：已存在
- 劉姿吟 #109：已存在
- 🏆豆干王🏆益全：與陳宜君同一張單，不重複補建

---

### 2026-06-11 F — 批次更新失效門號備註（第一批 5 筆）

**做了什麼：**
在 Supabase customers 表中，將下列 5 筆尚未綁定 LINE 的舊客戶 notes 欄位寫入「門號已無效」：
- 許釗榮 0905326922
- 呂宏琪 0909108361
- 陳琬菁 0911376026
- 陳小姐(王寶英) 0911200369
- 林寶芳 0910375662

**觸發原因：** 業主確認上述門號已無法聯繫，標記避免日後重複撥打。

---

### 2026-06-11 G — 批次更新失效門號備註（第二批 12 筆）

**做了什麼：**
在 Supabase customers 表中，將下列 12 筆舊客戶 notes 欄位追加「，門號已無效」（保留原有備註內容）：
- 黃善智 0983703252（原有備註保留）
- 梁以琪 0989793515
- 王玉枝 0983800807（原有備註保留）
- 方夏美 0982538623
- 楊志文 0989682924
- 李郡如 0988117772
- 羅佳伶 0988895959（原有備註保留）
- 徐文绮 0983252762
- 陳美蘭 0989094162
- 蔡政霖 0989169777
- 曹瑞顯 0988896859
- 江文靜 0988135203

**觸發原因：** 同上，業主確認門號失效，逐一追加標記。

---

### 2026-06-11 H — 補禁止「星期幾→自行推算日期」規則

**commit：** 0d31fe6

**做了什麼：**
rule 1833 原本只禁止「客人給日期→Claude 自行推算星期幾」，補上反向規則：
「客人說星期幾（如星期六）→ 必須查日期對照表找對應日期，不可自行計算」，附具體反例。

**觸發原因：**
Ling（U146cdae...）說「星期六 11:00」，Claude 自行計算成 06/14（週日），應為 06/13（週六）。
工具驗證 06/14 週日為合法時間，Claude 先確認再自我否定，業主手動暫停機器人注入訂單收尾。

---

### 2026-06-11 I — 補節日名稱必須查表規則（三向禁止）

**commit：** 03330c7

**做了什麼：**
rule 1833「雙向禁止」升級為「三向禁止」，新增第三條：
「客人說節日名稱（如端午節後）→ 必須查日期對照表找 MM/DD → 呼叫 check_ship_date，不可自行推算」
同步在 FAQ 區新增「節日詢問出貨」標準處理方式。

**觸發原因：**
陳宜君（U019393326700c0e6518d91a3256d099e）說「端午節過後」，bot 未查日期對照表（已標注 06/19 端午節），
自行推算出貨/收件日且說顛倒，客人說「顛倒了吧」，業主手動介入注入訂單。

---

### 2026-06-13 A — 移除 [RAW] log 300字截斷

**commit：** 7bbd438

**做了什麼：**
`app.py` 第 3623 行，`[RAW]` log 從 `raw[:300]` 改為 `raw`，完整記錄每則機器人回覆。

**觸發原因：**
宅配訂單確認訊息約 400–450 字，超過 300 字被截斷，導致 Render log 看不到完整回覆內容，難以除錯。

---

### 2026-06-13 B — 自取取貨時間立即驗證 + 詢問時段依週日修正

**commit：** e85ea6b

**做了什麼：**
1. SYSTEM_TEXT rule 12 新增【取貨時間立即驗證】：客人說出取貨時間後必須立即呼叫 `validate_pickup_time`，不可先說「完美」「已記下」再等資料齊全才驗；驗證通過才繼續，不通過立即告知改約
2. 新增【詢問取貨時間依取貨日列出正確時段】：取貨日已知為週日→只列 08:00–13:30；週一至六→列上下午；取貨日未知→列完整並備注週日僅上午
3. 兩處「平行呼叫 calc_pickup + validate_pickup_time」說明同步更新，移除 validate 從此處呼叫的描述

**觸發原因：**
蔡秀娟（Ua945446...）2026-06-13 對話：客人說「明天上午13:30前取」，機器人說「完美！已記下」，等客人確認電話後才驗證，發現 13:30 是打烊時間才改口「抱歉」；且當天明確是週日，機器人卻問「下午幾點？（16:00–18:00）」，誤導客人。

---

### 2026-06-14 A — 手動補建訂單（張齡方 #124）

**做了什麼：**
張齡方（0920975992）今天由益全代訂宅配，機器人成立訂單但未寫入 Supabase。
手動補建訂單 #124：豆干絲 10包 × 70元 = 700元，運費 225元，總計 925元，出貨日 2026-06-15，地址：南投縣竹山鎮延和里和育巷367-13號，status=pending。

---

### 2026-06-14 B — 黃明俊備註更新

**做了什麼：**
黃明俊（0979857915）今日通知取消 6/15 宅配，確認 Supabase 本無訂單記錄（機器人未寫入）。
在 customers 備註欄寫入：「2026/6/14 宅配取消放鳥，也不主動通知」。

---

### 2026-06-16 A — 客人問運費被閃避 + 疑問句被誤判成下單

**commit：** 未上傳（等業主說「上傳」）

**做了什麼：**
SYSTEM_TEXT rule 8 兩處外科手術式改動：
1. 第 1672–1679「嚴禁提前報價」區塊末尾新增例外：客人主動問「包郵嗎/免運嗎/運費多少/運費是XXX？」時，因運費只與包數有關，**必須立即呼叫 calc_delivery 實算後直接回答運費**，不可閃避或只回「已記下」。仍保留原意（禁止不呼叫工具就自行估價或附和客人）。
2. create_order 步驟後新增【疑問句不等於下單確認】：客人訊息為疑問句（句尾？/是…嗎/多少）時視為詢問，**即使資料齊全也絕對不可 create_order / create_pickup**，必須等明確肯定下單語（確認/就這樣/下單/可以…）才成立訂單，附孫維悌實例反例。

**觸發原因：**
孫維悌（U82d730d3dba22f32c1fda69df561f31c，0905625241）對話：
- 10:59 問「豆干絲10包，包郵？」→ 機器人受 rule 1672「confirm 前不可報運費」限制，閃避只回「已記下」，沒回答。
- 11:09 客人被迫重問「運費是225元？」→ 機器人這次資料已齊全，把這個**疑問句誤判成下單確認**，直接 create_order 成立訂單。
- 11:10 客人說「稍等，我再看看！」證明根本沒要下單。

根因：機器人遇到運費疑問句時，不是當「要回答的問題」，而是當「流程推進訊號」——該答的不答，不該成立的卻成立。兩條規則（純詢問要答 vs confirm 前不可報運費）衝突，已收窄後者。

**驗證：** 本機重放兩句話，T1「包郵？」order_created=False/calc_called=True（直接答運費）；T2「運費是225元？」order_created=False（不成立訂單，改問是否確認）。皆 PASS，測試腳本已刪。

---

### 2026-06-16 B — 善後孫維悌錯誤訂單 #132

**做了什麼：**
查 Supabase 確認 2026-06-16 A 那張誤成立的訂單確實寫入：**訂單 #132**（孫維悌/0905625241，宅配豆干絲10包，925元，ship_date 2026-06-22，原 status=pending）。客人實際說「稍等我再看看」未下單，將 status 改為 **cancelled**（不刪除，保留記錄）。Redis 對話未動，客人若回頭說要訂可接續。

---

## 待處理問題

無。
